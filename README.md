# Менеджер завдань

Повнофункціональний застосунок для управління завданнями з React фронтендом та Express бекендом. Включає Kanban board з drag'n'drop, редагування задач, фільтрацію та повне тестове покриття.

## Структура проєкту

Проєкт складається з двох частин:

### Backend (API сервер)

```
├── server.ts              # Головний файл сервера
├── models/                # Sequelize моделі
│   ├── database.ts        # Ініціалізація Sequelize
│   ├── Task.model.ts      # Модель задачі
│   ├── User.model.ts      # Модель користувача
│   └── index.ts           # Експорт моделей та налаштування зв'язків
├── controllers/           # Контролери
│   └── task.controller.ts
├── routes/                # Роути
│   └── task.routes.ts
├── services/              # Бізнес-логіка
│   └── task.service.ts
├── types/                 # TypeScript типи
│   └── task.types.ts
└── tests/                 # Інтеграційні тести
    ├── setup.ts           # Налаштування тестів
    └── tasks.test.ts      # Тести для API endpoints
```

### Frontend (React застосунок)

```
src/
├── features/
│   └── tasks/              # Фіча tasks
│       ├── api.ts           # API запити
│       ├── types.ts         # Типи даних
│       ├── components/      # Компоненти фічі
│       │   ├── CreateTaskForm.tsx
│       │   ├── EditTaskForm.tsx      # Форма редагування задач
│       │   ├── KanbanBoard.tsx       # Kanban board з drag'n'drop
│       │   ├── KanbanBoard.test.tsx  # Тести для Kanban board
│       │   ├── TaskCard.tsx
│       │   └── TaskCard.test.tsx
│       └── pages/           # Сторінки
│           ├── TasksListPage.tsx
│           ├── TasksListPage.test.tsx
│           ├── CreateTaskPage.tsx
│           ├── CreateTaskPage.test.tsx
│           ├── TaskDetailsPage.tsx
│           └── TaskDetailsPage.test.tsx
├── models/                 # Моделі даних
├── router.tsx              # Конфігурація роутера
└── App.tsx                 # Головний компонент
```

## Встановлення

**Важливо:** Перед запуском проєкту обов'язково встановіть залежності:

```bash
npm install
```

Після встановлення залежностей автоматично ініціалізується Husky для pre-commit hooks.

**Якщо ви бачите помилку типу "tsx: not found" або "command not found"** — це означає, що залежності не встановлені. Запустіть `npm install` і спробуйте знову.

**Якщо при коміті виникає помилка "The 'jiti' library is required"** — це означає, що потрібно встановити залежності. Бібліотека `jiti` вже додана в `package.json` і встановиться при виконанні `npm install`. Якщо помилка залишається, переконайтеся, що ви виконали `npm install` після додавання нових залежностей.

## База даних

Проєкт використовує **SQLite** базу даних через **Sequelize ORM**.

- **Основна БД**: `database.sqlite` (створюється автоматично при першому запуску)
- **Тестова БД**: `test-database.sqlite` (використовується для тестів)

### Моделі

- **Task** - модель задачі з полями: id, title, description, status, priority, deadline, assigneeId
  - Статуси: `todo`, `in-progress`, `review`, `done`
  - Пріоритети: `low`, `normal`, `high`
- **User** - модель користувача з полями: id, name, email

### Зв'язки між моделями

- Кожна задача (Task) має рівно одного виконавця (assignee) через `assigneeId`
- Користувач (User) може мати багато задач

База даних автоматично синхронізується при запуску сервера.

## Запуск проєкту

### Backend API сервер

Запустіть Express сервер:

```bash
npm run start
```

Або з автоперезавантаженням (watch mode):

```bash
npm run start:dev
```

Сервер запуститься на `http://localhost:3000`

При першому запуску буде створено файл `database.sqlite` з необхідними таблицями.

**Зупинка сервера:**

Для зупинки сервера використовуйте один з наступних способів:

1. **Через npm скрипт (рекомендовано):**

   ```bash
   npm run stop
   ```

2. **Вручну через Ctrl+C:**
   - Якщо сервер запущений у поточному терміналі, натисніть `Ctrl+C`

3. **Через знаходження та завершення процесу:**

   ```bash
   # Знайти процес
   ps aux | grep "tsx server"

   # Завершити процес за PID
   kill <PID>

   # Або примусово завершити
   kill -9 <PID>
   ```

**Зупинка сервера:**

Для зупинки сервера використовуйте один з наступних способів:

1. **Через npm скрипт (рекомендовано):**

   ```bash
   npm run stop
   ```

2. **Вручну через Ctrl+C:**
   - Якщо сервер запущений у поточному терміналі, натисніть `Ctrl+C`

3. **Через знаходження та завершення процесу:**

   ```bash
   # Знайти процес
   ps aux | grep "tsx server"

   # Завершити процес за PID
   kill <PID>

   # Або примусово завершити
   kill -9 <PID>
   ```

**Зупинка сервера:**

Є кілька способів зупинити сервер:

1. **Якщо сервер запущений в терміналі (рекомендовано):**
   - Натисніть `Ctrl + C` в терміналі, де запущений сервер
   - Це найбезпечніший спосіб, який коректно завершує процес

2. **Використання npm скрипта:**

   ```bash
   npm run stop
   ```

   Це зупинить всі процеси `tsx server.ts` або `tsx watch server.ts`

3. **Якщо сервер запущений у фоновому режимі або в іншому терміналі:**

   ```bash
   npm run stop
   ```

   Або вручну:

   ```bash
   pkill -f "tsx server.ts"
   ```

4. **Знайти та зупинити процес вручну:**

   ```bash
   # Знайти процес
   ps aux | grep "tsx server.ts"

   # Зупинити за PID (замініть <PID> на номер процесу)
   kill <PID>
   ```

### Frontend React застосунок

В іншому терміналі:

```bash
npm run dev
```

Застосунок буде доступний на `http://localhost:5173` (або іншому порту, якщо 5173 зайнятий)

## API Endpoints

### GET /tasks

Отримання всіх завдань з підтримкою фільтрів.

**Query параметри (опціональні):**

- `createdAt` - фільтр за датою створення
- `status` - фільтр за статусом (`todo`, `in-progress`, `review`, `done`)
- `priority` - фільтр за пріоритетом (`low`, `normal`, `high`)

**Приклади:**

```bash
# Отримати всі завдання
GET /tasks

# Фільтр за статусом
GET /tasks?status=in-progress

# Фільтр за пріоритетом
GET /tasks?priority=high

# Комбінована фільтрація
GET /tasks?status=in-progress&priority=high
```

**Відповідь:**

```json
[
  {
    "id": "1",
    "title": "string",
    "description": "string",
    "createdAt": "2025-01-21T08:00:00.000Z",
    "status": "todo",
    "priority": "high",
    "deadline": "2025-12-31",
    "assigneeId": 1,
    "assignee": {
      "id": 1,
      "name": "John Doe",
      "email": "john@example.com"
    }
  }
]
```

### GET /tasks/:id

Отримання завдання за ID з деталями виконавця.

**Відповідь (200):**

```json
{
  "id": "1",
  "title": "string",
  "description": "string",
  "createdAt": "2025-01-21T08:00:00.000Z",
  "status": "todo",
  "priority": "high",
  "deadline": "2025-12-31",
  "assigneeId": 1,
  "assignee": {
    "id": 1,
    "name": "John Doe",
    "email": "john@example.com"
  }
}
```

**Помилка (404):**

```json
{
  "error": "Task not found"
}
```

### POST /tasks

Створення нового завдання.

**Request Body:**

```json
{
  "title": "string", // обов'язкове, мінімум 1 символ
  "description": "string", // обов'язкове, мінімум 1 символ (не може бути порожнім)
  "status": "todo", // обов'язкове: "todo" | "in-progress" | "review" | "done"
  "priority": "high", // обов'язкове: "low" | "normal" | "high"
  "deadline": "2025-12-31", // опціональне, може бути null
  "assigneeId": 1 // опціональне, ID користувача-виконавця
}
```

**Важливо:** Поле `description` є обов'язковим і має містити мінімум 1 символ. Порожній рядок не приймається.

**Відповідь (201):**

```json
{
  "id": "1",
  "title": "string",
  "description": "string",
  "createdAt": "2025-01-21T08:00:00.000Z",
  "status": "todo",
  "priority": "high",
  "deadline": "2025-12-31",
  "assigneeId": 1,
  "assignee": {
    "id": 1,
    "name": "John Doe",
    "email": "john@example.com"
  }
}
```

**Помилка валідації (400):**

```json
{
  "error": "Validation error",
  "details": [
    {
      "path": ["title"],
      "message": "Title is required"
    }
  ]
}
```

### PUT /tasks/:id

Оновлення завдання.

**Request Body (всі поля опціональні):**

```json
{
  "title": "string", // опціональне
  "description": "string", // опціональне
  "status": "in-progress", // опціональне
  "priority": "normal", // опціональне
  "deadline": "2025-12-31", // опціональне, може бути null
  "assigneeId": 2 // опціональне, ID користувача-виконавця
}
```

**Відповідь (200):** Оновлене завдання з деталями виконавця

**Помилки:**

- `404` - завдання не знайдено
- `400` - помилка валідації

### DELETE /tasks/:id

Видалення завдання.

**Відповідь (200):**

```json
{
  "message": "Task deleted successfully"
}
```

**Помилка (404):**

```json
{
  "error": "Task not found"
}
```

## Обробка помилок

API повертає наступні HTTP статуси:

- **200** - Успішний запит
- **201** - Ресурс створено
- **400** - Помилка валідації (невірні параметри або body)
- **404** - Ресурс не знайдено
- **500** - Внутрішня помилка сервера

## Тестування

### Backend інтеграційні тести

Проєкт містить повний набір інтеграційних тестів для всіх API endpoints з використанням **Jest** та **Supertest**.

```bash
# Запуск всіх тестів (backend + frontend)
npm test

# Запуск тільки backend тестів
npm run test:backend

# Запуск тестів в watch режимі
npm run test:watch

# Запуск тестів з покриттям коду
npm run test:coverage
```

**Тести покривають:**

- ✅ Успішні сценарії (200, 201)
- ✅ Помилкові сценарії (400, 404)
- ✅ Валідацію даних
- ✅ Фільтрацію задач
- ✅ Роботу з виконавцями (assignee)
- ✅ CRUD операції

Тести використовують окрему тестову базу даних (`test-database.sqlite`), яка автоматично очищається перед кожним запуском тестів.

**Структура backend тестів:**

- `tests/tasks.test.ts` - інтеграційні тести для всіх `/tasks/*` ендпоінтів (36 тестів)
- `tests/setup.ts` - налаштування тестового середовища та БД

**Статистика backend тестів:**

- ✅ 37+ тестів проходять успішно
- ✅ Покриття всіх CRUD операцій
- ✅ Покриття валідації та обробки помилок
- ✅ Тести для статусу 'review'
- ✅ Тести для фільтрації за всіма статусами

### Frontend тести

Frontend тести використовують **Vitest** та **React Testing Library**.

```bash
# Запуск всіх тестів (backend + frontend)
npm test

# Запуск тільки frontend тестів
npm run test:frontend
```

**Структура frontend тестів:**

- `src/features/tasks/pages/TasksListPage.test.tsx` - тести для списку завдань з Kanban board (12 тестів)
- `src/features/tasks/pages/CreateTaskPage.test.tsx` - тести для форми створення (10 тестів)
- `src/features/tasks/pages/TaskDetailsPage.test.tsx` - тести для деталей завдання з edit mode (13 тестів)
- `src/features/tasks/components/TaskCard.test.tsx` - тести для картки завдання (10 тестів)
- `src/features/tasks/components/KanbanBoard.test.tsx` - тести для Kanban board (7 тестів)

**Статистика frontend тестів:**

- ✅ 52+ тести проходять успішно
- ✅ Покриття всіх компонентів та сторінок
- ✅ Покриття валідації форм та навігації
- ✅ Покриття Kanban board та drag'n'drop
- ✅ Покриття edit mode з перевіркою початкових значень

**Frontend тести перевіряють:**

**Список завдань (TasksListPage):**

- ✅ Елементи у списку відображаються коректно (усі потрібні поля присутні)
- ✅ При порожньому списку — відображається empty state
- ✅ При помилці — показується error message
- ✅ Стан завантаження (loading state)
- ✅ Функціонал видалення завдань через кнопку на картці

**Створення завдання (CreateTaskPage):**

- ✅ Кнопка Submit має стан disabled, якщо форма порожня або невалідна
- ✅ Якщо форма валідна (назва та опис заповнені), кнопка стає enabled
- ✅ При помилках валідації з'являються відповідні error messages під усіма інпутами
- ✅ Навігація після успішного створення
- ✅ Перевірка валідації для всіх обов'язкових полів (title, description)
- ✅ Підтримка статусу 'review' в формі створення

**Деталі завдання (TaskDetailsPage):**

- ✅ Відображення всіх полів завдання
- ✅ Обробка стану завантаження
- ✅ Обробка помилок (завдання не знайдено)
- ✅ Навігація назад
- ✅ Edit mode з формою редагування
- ✅ Початкові значення інпутів відповідають даним задачі
- ✅ Кнопка "Зберегти зміни" disabled, якщо форма невалідна або не змінена
- ✅ Валідація форми редагування з відображенням помилок

**Kanban Board:**

- ✅ Відображення всіх 4 колонок (To Do, In Progress, Review, Done)
- ✅ Правильне розподілення задач по колонках за статусом
- ✅ Відображення кількості задач у кожній колонці
- ✅ Empty state для порожніх колонок
- ✅ Drag'n'drop функціонал для зміни статусу
- ✅ Кнопки швидкої зміни статусу

**Список завдань (TasksListPage):**

- ✅ Перемикання між Kanban board та списком
- ✅ Елементи у списку відображаються коректно (усі потрібні поля присутні)
- ✅ При порожньому списку — відображається empty state
- ✅ При помилці — показується error message
- ✅ Стан завантаження (loading state)
- ✅ Функціонал видалення завдань через кнопку на картці

**Картка завдання (TaskCard):**

- ✅ Відображення всіх необхідних полів
- ✅ Форматування дат
- ✅ Навігація при кліку
- ✅ Функціонал видалення завдання (кнопка ×)

### Використання curl

```bash
# Отримати всі завдання
curl http://localhost:3000/tasks

# Створити завдання
curl -X POST http://localhost:3000/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Нове завдання",
    "description": "Опис завдання",
    "status": "todo",
    "priority": "high",
    "deadline": "2025-12-31"
  }'

# Отримати завдання за ID
curl http://localhost:3000/tasks/1

# Оновити завдання
curl -X PUT http://localhost:3000/tasks/1 \
  -H "Content-Type: application/json" \
  -d '{
    "status": "in-progress"
  }'

# Видалити завдання
curl -X DELETE http://localhost:3000/tasks/1
```

### Використання тестового скрипта

Запустіть готовий тестовий скрипт:

```bash
./test-api-simple.sh
```

## Frontend - Як перевірити роботу

### В браузері:

1. **Список завдань** (`/tasks` або `/`)
   - **Kanban Board** (за замовчуванням):
     - Відображаються 4 колонки: To Do, In Progress, Review, Done
     - Завдання автоматично розподіляються по колонках за статусом
     - Drag'n'drop: перетягніть завдання між колонками для зміни статусу
     - Кнопки зміни статусу: наведіть на завдання, щоб побачити кнопки для швидкої зміни статусу
     - Кожна колонка показує кількість завдань
   - **Список** (перемикач у верхньому правому куті):
     - Класичне відображення всіх завдань у списку
     - Перемикання між Kanban та Списком через кнопки в заголовку
   - Кнопка "Створити завдання" веде на `/tasks/create`
   - Клік по завданню веде на `/tasks/:id`

2. **Створення завдання** (`/tasks/create`)
   - Заповніть форму (назва та опис обов'язкові)
   - Виберіть статус: To Do, In Progress, Review, або Done
   - Кнопка "Створити завдання" disabled, якщо форма невалідна або не змінена
   - Після успішного створення - автоматичний перехід на `/tasks`
   - Перевірте валідацію (назва та опис обов'язкові, дедлайн не може бути в минулому)
   - Помилки валідації відображаються під кожним інпутом

3. **Редагування завдання** (`/tasks/:id`)
   - На сторінці деталей натисніть кнопку "Редагувати"
   - Форма заповнюється поточними даними завдання
   - Кнопка "Зберегти зміни" disabled, якщо форма невалідна або не змінена
   - Після збереження автоматично оновлюються дані на сторінці
   - Кнопка "Скасувати" повертає до перегляду без збереження

4. **Видалення завдання**
   - На картці завдання в списку є кнопка видалення (×) у правому верхньому куті
   - При натисканні з'являється підтвердження
   - Після видалення список автоматично оновлюється

5. **Деталі завдання** (`/tasks/:id`)
   - Відображається повна інформація про завдання
   - Кнопка "← Назад" повертає на `/tasks`
   - Кнопка "Редагувати" відкриває форму редагування

6. **Empty state**
   - Якщо немає завдань, має відобразитися повідомлення "Немає завдань"
   - В Kanban board кожна порожня колонка показує "Немає завдань"

7. **Error state**
   - Якщо зупинити backend сервер, має відобразитися повідомлення про помилку

## Доступні скрипти

### Backend

- `npm run start` - запуск Express сервера
- `npm run start:dev` - запуск сервера з автоперезавантаженням
- `npm run stop` - зупинка сервера (зупиняє всі процеси `tsx server.ts` або `tsx watch server.ts`)
- `npm run build:backend` - збірка backend (TypeScript → JavaScript в `/dist`)

### Frontend

- `npm run dev` - запуск dev сервера
- `npm run build:frontend` - збірка frontend (Vite bundle в `/dist`)
- `npm run preview` - перегляд зібраного проєкту
- `npm run build` - збірка обох частин проєкту (backend + frontend)

### Тестування

- `npm test` - запуск всіх тестів (backend + frontend)
- `npm run test:backend` - запуск тільки backend тестів (Jest)
- `npm run test:frontend` - запуск тільки frontend тестів (Vitest)
- `npm run test:watch` - запуск backend тестів в watch режимі
- `npm run test:coverage` - запуск backend тестів з покриттям коду

### Лінтинг та форматування

- `npm run lint` - перевірка коду ESLint
- `npm run lint:fix` - автоматичне виправлення помилок ESLint
- `npm run format` - форматування коду Prettier

### Інші

- `npm run server` - запуск json-server (старий API, якщо потрібен)

## Лінтинг та форматування коду

### ESLint

Проєкт налаштований з **ESLint** для перевірки якості коду:

- **Frontend**: підтримка React + TypeScript + JSX
- **Backend**: підтримка Node.js + TypeScript
- **Інтеграція з Prettier** для автоматичного форматування

Конфігурація знаходиться в `eslint.config.ts` (flat config format).

### Prettier

**Prettier** налаштований для автоматичного форматування коду:

- Конфігурація: `.prettierrc.json`
- Ігнорування файлів: `.prettierignore`

### Pre-commit hooks (Husky + lint-staged)

Проєкт використовує **Husky** та **lint-staged** для автоматичної перевірки коду перед комітом:

- ✅ Перевіряються лише staged файли
- ✅ Виконується автоматичне виправлення проблем ESLint (`--fix`)
- ✅ При неуспішному виконанні хуку коміт не створюється

**Налаштування:**

- `.husky/pre-commit` - скрипт pre-commit hook
- `package.json` → `lint-staged` - конфігурація для staged файлів

**Що перевіряється:**

- `*.{ts,tsx}` - ESLint + Prettier
- `*.{json,css,md}` - Prettier

## Frontend роути

- `/` - список завдань (Kanban board або список)
- `/tasks` - список завдань (Kanban board або список)
- `/tasks/create` - створення завдання
- `/tasks/:id` - деталі завдання з можливістю редагування

## Основні функції

### Kanban Board

- 📋 Візуалізація задач у вигляді колонок за статусами
- 🔄 Drag'n'drop для швидкої зміни статусу задач
- 🔘 Кнопки швидкої зміни статусу при наведенні на задачу
- 📊 Відображення кількості задач у кожній колонці
- 🎨 Адаптивний дизайн для різних розмірів екрану

### Редагування задач

- ✏️ Edit mode з формою редагування на сторінці деталей
- ✅ Валідація форми з відображенням помилок
- 🔒 Кнопка збереження disabled, якщо форма невалідна або не змінена
- 📝 Початкові значення автоматично заповнюються з даних задачі

### Статуси задач

- 📌 **To Do** - нові задачі
- 🚀 **In Progress** - задачі в роботі
- 👀 **Review** - задачі на перевірці
- ✅ **Done** - виконані задачі

## Технології

### Backend

- **Express** - веб-фреймворк
- **Sequelize** - ORM для роботи з базою даних
- **SQLite** - база даних
- **TypeScript** - типізація
- **Zod** - валідація даних
- **Morgan** - логування HTTP запитів (тільки в dev mode)
- **CORS** - підтримка cross-origin запитів
- **Jest** - фреймворк для тестування
- **Supertest** - HTTP assertions для тестування API

### Frontend

- **React** - UI бібліотека
- **React Router** - маршрутизація
- **React Hook Form** - управління формами
- **Zod** - валідація форм
- **TypeScript** - типізація
- **Vite** - збірщик
- **Vitest** - тестування
- **React Testing Library** - тестування React компонентів

### Інструменти розробки

- **ESLint** - лінтер для JavaScript/TypeScript
- **Prettier** - форматер коду
- **Husky** - Git hooks
- **lint-staged** - запуск лінтера на staged файлах

## Особливості реалізації

### Backend

- ✅ Використання ES Modules (не CommonJS)
- ✅ camelCase для змінних і функцій
- ✅ Валідація через Zod для request body та query параметрів
- ✅ Обробка помилок з правильними HTTP статусами (400/404/500)
- ✅ Зберігання даних в SQLite базі даних через Sequelize
- ✅ Middleware для логування (morgan тільки в dev mode) та CORS
- ✅ Розділення на контролери, сервіси та роути
- ✅ Повне покриття API інтеграційними тестами (37+ тестів)
- ✅ Зв'язок між моделями Task та User (один-до-багатьох)
- ✅ Окрема тестова база даних для тестів
- ✅ Підтримка 4 статусів: todo, in-progress, review, done

### Frontend

- ✅ Kanban board з drag'n'drop для зміни статусу задач
- ✅ Перемикання між Kanban board та списком задач
- ✅ Редагування задач з валідацією та перевіркою змін
- ✅ Повне покриття frontend компонентів unit тестами (52+ тести)
- ✅ Валідація форм з відображенням помилок під усіма інпутами
- ✅ Кнопки submit disabled, якщо форма невалідна або не змінена
- ✅ Edit mode з початковими значеннями, що відповідають даним задачі
- ✅ Коректне відображення всіх полів завдання
- ✅ Функціонал видалення завдань з підтвердженням

### Інструменти та якість коду

- ✅ ESLint + Prettier для якості коду
- ✅ Pre-commit hooks для автоматичної перевірки
- ✅ Окремі build процеси для frontend та backend
- ✅ Оптимізований tsconfig.json з тільки потрібними опціями

## Production Build

### Frontend

```bash
npm run build:frontend
```

Створює оптимізований bundle в `/dist` за допомогою Vite.

### Backend

```bash
npm run build:backend
```

Компілює TypeScript файли в JavaScript в `/dist`.

### Обидва

```bash
npm run build
```

Збирає обидві частини проєкту.

**Важливо:** Директорія `/dist` додана в `.gitignore` і не зберігається в репозиторії.

## Примітки

- База даних SQLite створюється автоматично при першому запуску сервера
- Файли бази даних (`*.sqlite`) додані в `.gitignore` і не зберігаються в репозиторії
- Для production використання рекомендується використовувати PostgreSQL або іншу production-ready БД
- Frontend налаштований на роботу з Express API
- Інтеграційні тести автоматично очищають тестову БД перед кожним запуском
- Pre-commit hooks автоматично перевіряють та виправляють код перед комітом
- Всі тести повинні проходити перед комітом (якщо налаштовано CI/CD)
